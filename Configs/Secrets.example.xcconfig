# Copy this file to Configs/Secrets.xcconfig (excluded from source control) and fill in the values.
# Note: Use $(SLASH)$(SLASH) for // in URLs to avoid xcconfig comment parsing issues
SLASH = /
SENTRY_DSN = https:$(SLASH)$(SLASH)<sentry-dsn-host>$(SLASH)<project-id>
SENTRY_ENVIRONMENT = <production|staging|development>

STATSIG_SDK_KEY = <statsig-sdk-key>
STATSIG_ENVIRONMENT = <production|staging|development>

TMDB_API_KEY = <tmdb-api-key>
